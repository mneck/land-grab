class Player1 {
  constructor() {
    // this.tile = tiles;
  }
  //draws Player1 Farms & Settlements
  // build() {
  //   console.log("this tile");

  // this.tile;
  // let cursorPosition = [mouseX, mouseY];

  // // if (mouseX <= this.tile.land.xposition && mouseX >=)
  // function mouseClicked(event) {
  //   if (mouseX < 60 && mouseY < 60) {
  //     console.log("first tile!");
  //   }
  // }
  // }
}

//button click for Player1
// if (mouseButton === LEFT) {

// leftPressed = true;
// this.tile.forest = this.tile.player1Farm;
//tile should be farm
// if (leftPressed) {
//   leftPressed = false;

// if (mouseButton === LEFT && this.tile.land) {
//   leftPressed = true;
//   this.tile.land = this.tile.player1Farm;
//   //tile should be farm
//   if (leftPressed) {
//     leftPressed = false;
//   }
// }

// if (mouseButton === RIGHT && this.tile.forest) {
//   rightPressed = true;
//   this.tile.forest = this.tile.mine;
//   if (rightPressed) {
//     rightPressed = false;
//   }
// }

// if (mouseButton === RIGHT && this.tile.land) {
//   rightPressed = true;
//   this.tile.land = this.tile.mine;
//   if (rightPressed) {
//     rightPressed = false;
//   }
// }

/* dist between (mouseX, mouseY, tile[i].xposition, tile[i].yposition
    if ( mouseX === this.tile.xPosition && mouseY )
    

      */

//     if (mouseButton === LEFT && this.tile.land) {
//       leftPressed = true;
//       this.tile.land = this.tile.player1Farm;
//       //tile should be farm
//       if (leftPressed) {
//         leftPressed = false;
//       }
//     }

//     if (mouseButton === RIGHT && this.tile.forest) {
//       rightPressed = true;
//       this.tile.forest = this.tile.mine;
//       if (rightPressed) {
//         rightPressed = false;
//       }
//     }

//     if (mouseButton === RIGHT && this.tile.land) {
//       rightPressed = true;
//       this.tile.land = this.tile.mine;
//       if (rightPressed) {
//         rightPressed = false;
//       }
//     }
//   }
// }

// builds resort settlements & farms
// resort settlements get +1 income from
// adjacent water & mountain tiles
// class Player2 {
//   constructor() {
//     this.col = 100;
//     this.row = 100;
//     this.image;
//   }
//   //draws Player2 Farms & Settlements
//   mouseClicked() {
//     if (mouseButton === LEFT) {
//       leftPressed = true;
//       //background should be farm
//       background(255);
//     }
//     if (mouseButton === RIGHT) {
//       rightPressed = true;
//       //background should be town
//       background(0);
//     }
//   }
// }
